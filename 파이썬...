가상환경만들기... 폴더로 들어가서... dir, cd (폴더이름)

conda activate
Conda create –n py37 python=3.7 numpy pandas matplotlib keras
Conda activate py37
가상환경이름 py37

(jupyter notebook설치)
conda install –c conda-forge jupyter notebook
(ipykernel 연결)
pip install ipykernel
python –m ipykernel install --user --name keras --display-name “[test]”
추가적으로 구글에서 필요한 패키지 찾는 방법(conda install “package이름”)
